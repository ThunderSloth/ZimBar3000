--------------------------------------------------------------------------------
--   CONFIGURATION
--------------------------------------------------------------------------------
function on_alias_smugs_configure()
	dofile(SMU_PATH:gsub("\\([A-Za-z_]+)\\$", "\\shared\\").."zconfig.lua")
end
